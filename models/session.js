const sessionSchema = {
    sessionId: 'string',
    topic: 'string',
    time: 'date',
    participants: 'array'
};

module.exports = sessionSchema;

//Sessions (SessionId, topic, time, participants)
